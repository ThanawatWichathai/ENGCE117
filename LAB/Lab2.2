#include <stdio.h>
#include <string.h>

char *reverse( char str1[] ) ;

int main() {

    char text[50] ;
    char *out ;
    out = reverse( text ) ;
    printf( "Input = %s\n", text ) ;
    printf( "Output = %s", out ) ;

    return 0 ;
}//end function

char *reverse( char str1[] ) {

	char *Reverse = new char[50] ;
    gets( str1 ) ;

    int lenght = strlen( str1 ) ;
    
    for( int i = 0 ; i < lenght ; i++ ) {
        Reverse[i] = str1[ lenght - 1 - i ] ;
    }//end for
    
    return Reverse ;
}//end function
