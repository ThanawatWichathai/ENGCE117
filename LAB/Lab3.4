#include <stdio.h>

struct student {
    char name[ 20 ] ;
    int age ;
    char sex ;
    float gpa ;
} ;

void GetStudent( struct student child[][10], int *room  ) ;

int main() {

    struct student children[20][10] ;
    int group = 0 ;

    GetStudent( children, &group ) ;

    for( int i = 0 ; i < group ; i++ ) {
        printf( "Room Number: %d\n", i + 1 ) ;

        for( int d = 0 ; d < 10 ; d++ ) {
            printf( "%d %4s %3d %3c %3.2f\n", d + 1, children[i][d].name, children[i][d].age, children[i][d].sex, children[i][d].gpa ) ;
        }///end for
    }//end for //Display

    return 0 ;
}//end function

void GetStudent( struct student child[][10], int *room  ) {
    printf( "Please Enter Room(1-20): " ) ;
    if( scanf( "%d", room ) != 1 ) {
        return ;
    }//end if
    

    for( int i = 0 ; i < *room ; i++ ) {
        printf( "Room Number: %d\n", i + 1 ) ;

        for( int d = 0 ; d < 10 ; d++ ) {
            printf( "Student %d (name age sex gpa): ", d+1 ) ;
            scanf( "%19s %d %c %f", child[i][d].name, &child[i][d].age, &child[i][d].sex, &child[i][d].gpa ) ;
        }//end for
    }//end for //Input data

    for( int i = 0 ; i < *room ; i++ ) {
        printf( "Room Number: %d\n", i+1 ) ;

        for( int d = 0 ; d < 10 ; d++ ) {
            printf( "%d %4s %3d %3c %3.2f\n", d + 1, child[i][d].name, child[i][d].age, child[i][d].sex, child[i][d].gpa ) ;
        }///end for
    }//end for //Display 

}//end function
