#include <stdio.h>
#include <string.h>

struct student {
    char name[20] ;
    int age ;
    char sex ;
    float gpa ;
} typedef stds ;

void upgrade( struct student child ) ;

int main() {

    struct student aboy ;
    strcpy( aboy.name, "Thanawat" ) ;
    aboy.age = 18 ;
    aboy.sex = 'F' ;
    aboy.gpa = 3.00 ;
    printf( "%.2f\n", aboy.gpa ) ;

    upgrade( aboy) ;

    return 0 ;
}//end function

void upgrade( struct student child ) {
    if( child.sex == 'M' ) {
        child.gpa = ( child.gpa * 0.1 ) + child.gpa ;
    }else {
        child.gpa = ( child.gpa * 0.2 ) + child.gpa ;
    }//end if

    printf( "%.2f\n", child.gpa ) ;

}//end function
